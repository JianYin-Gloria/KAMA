% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/KAMA.R
\name{KAMA_meta_analysis_select}
\alias{KAMA_meta_analysis_select}
\title{KAMA Meta-Analysis Based Variable Selection}
\usage{
KAMA_meta_analysis_select(
  kappa_set,
  tau_set,
  M,
  q = 0.1,
  genetic_info = NULL,
  rep = 1,
  Uj_thre = 0.51,
  seed = 111
)
}
\arguments{
\item{kappa_set}{A numeric matrix of kappa statistics. Rows correspond to variables,
and columns correspond to studies.
\strong{Note:} The order of variables (rows) and studies (columns) must strictly match that of \code{tau_set}.}

\item{tau_set}{A numeric matrix of tau statistics. Rows correspond to variables,
and columns correspond to studies.
\strong{Note:} The order of variables (rows) and studies (columns) must strictly match that of \code{kappa_set}.}

\item{M}{Integer. The number of knockoffs used. Must be >= max(kappa_set).}

\item{q}{Numeric. The FDR control level (default 0.1).}

\item{genetic_info}{Optional data frame or matrix containing additional information (e.g., SNP IDs, positions) to be appended to the results.}

\item{rep}{Integer. Number of repetitions for stability selection (default 1).}

\item{Uj_thre}{Numeric. Stability threshold for selection (default 0.51).}

\item{seed}{Integer. Random seed for reproducibility (default 111).}
}
\value{
A data frame of the \strong{selected} variables containing:
\itemize{
\item \code{genetic_info}: (If provided) The appended metadata.
\item \code{kappa_set}: The kappa statistics for the selected variables.
\item \code{tau_set}: The tau statistics for the selected variables.
\item \code{qvalue}: KAMA's q-value for meta-analysis FDR control.
}
Returns an empty data frame if no variables are selected.
}
\description{
Performs KAMA meta-analysis and selects variables based on FDR control and stability thresholds.
}
